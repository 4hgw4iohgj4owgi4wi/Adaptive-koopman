# Ch4--Ch7 Balanced Proposed Revision Table

Target source: `manuscript_en_ch4_reviewfix_2026_05_26.tex`

Review source: `ch4_ch5_ch6_ch7_review_2026-05-26.md`

Status: proposal only. No `.tex` manuscript text has been changed.

## Purpose of This Version

This version is more compact than the 32-row detailed table, but more complete than the 14-package compact table. It keeps one row for every necessary proof boundary, section-level language issue, experiment cluster, caption/table-note cluster, overclaim risk, and Ch6--Ch7 structure change.

## Balanced Table

| ID | Priority | Coverage | Current Issue to Remove or Replace | Proposed Exact Wording or Rule | Reason to Keep |
|---|---:|---|---|---|---|
| B01 | P1 | Ch4 small-gain condition | `\alpha_c:=\alpha-c_w...` uses an undefined `\alpha`. | `Let $\alpha_{\mathrm{cert}}>0$ denote the nominal certificate decrease rate before absorbing the state-dependent residual term, and assume $\alpha_c:=\alpha_{\mathrm{cert}}-c_w\bar w_\chi^2L_\chi^2>0$.` | Closes a symbol-definition gap without changing the proof logic. |
| B02 | P1 | Ch4 mode switching | `\rho_P,c_P` are defined but not visibly carried into average contraction. | Add after mode comparability: `The failure-growth constants $\rho_f$ and $c_f$ include the worst-case jump induced by the mode-comparison factors $\rho_P$ and $c_P$.` | Prevents the reader from thinking switching jumps were omitted. |
| B03 | P1 | Ch4 IRSP boundary | Sampled IRSP and continuous input-envelope coverage are not separated clearly enough. | Add after the norm-certificate sentence: `Without the norm certificate, the boundedness claim is restricted to the sampled control envelope $\mathcal U_s$ used in the controller audit.` | Keeps the theorem precise and avoids overclaiming continuous-input coverage. |
| B04 | P0 | Ch5 headings | Ch5 section and subsection titles remain Chinese in an English manuscript. | Translate all Ch5 titles: `Experimental Results and Analysis`; `Experiment Groups and Baseline Protocol`; `Koopman, Delay, and FDI/FTC Mechanism Evidence`; `Main Comparison Results`; `Speed Sensitivity and Same-Initial-Condition Sine Diagnostics`; `High-Curvature Hairpin Comparison`; `Core Module Ablation`; `Stability-Certificate Statistics`. | Fixes visible manuscript-completion risk. |
| B05 | P0 | Ch5 opening protocol | The opening paragraph is too long, lists all experiments in one sentence, and mixes statistical levels. | Replace the opening with the block in `B05 Replacement Block` below. | Shortens the chapter opening while preserving all experiment categories. |
| B06 | P0 | Data-source note | Remove internal process words: `最新`, `参数重新生成`, `调参结果`, `回填`, and `若后续要求...需要重新运行`. | `The paired statistical tables and the same-initial-condition diagnostics are reported separately because they differ in seed count and controller configuration. The single-seed diagnostics are used only to inspect trajectory-level behavior and are not merged into the paired statistics. A full multi-seed evaluation of the updated diagnostic controller settings is left for future work.` | Turns project notes into publication language and preserves statistical honesty. |
| B07 | P1 | Baseline protocol | `地板基线` is informal; AKE comparison may sound like a full reproduction of the original paper. | `\AKE{} is a task-aligned mechanism baseline derived from the adaptive Koopman embedding idea of Singh et al.~\cite{singh2025adaptiveKoopman}. \ZOHcons{} is a low-order ZOH consistency baseline used to expose degradation without learned prediction or network-aware protection.` | Clarifies fairness and removes informal baseline wording. |
| B08 | P0 | Repeated result style | Repeated `客观结果是` and `造成上述结果的原因是` sound like internal review notes. | Replace `客观结果是` with `Figure~... shows that ...` or `Table~... shows that ...`; replace `造成上述结果的原因是` with `This behavior is consistent with ...` or `These results indicate that ...`. | Makes Ch5 read like a journal manuscript and weakens unsupported causality. |
| B09 | P0 | Internal-process terms | Remove `最新`, `调参`, `回填`, `主证据`, `原实验`, `内部审稿材料`, `当前核心`. | Use `diagnostic`, `reported separately`, `not merged into paired statistics`, `mechanism-oriented evidence`, `updated diagnostic controller settings`, or delete the term when it is only workflow narration. | Prevents reviewer-facing text from exposing drafting history. |
| B10 | P1 | Training-loss evidence | `证明` is too strong for a loss plot. | Caption sentence: `The figure checks convergence and excludes obvious divergence rather than serving as a formal proof of closed-loop stability.` Result sentence: `The losses enter a plateau after about 60 epochs, and the last-10-epoch validation losses remain at the same order as the training losses.` | Converts an overclaim into an evidence statement. |
| B11 | P1 | Koopman prediction and IRSP | The paragraph risks implying bilinear Koopman is uniformly more accurate. | `The main point is not that the bilinear Koopman model uniformly minimizes open-loop rollout error. Rather, it provides comparable short-horizon prediction while IRSP reduces the sampled effective spectral radius from values above one to below one.` | Matches the reported overlapping confidence intervals at the MPC horizon. |
| B12 | P1 | K1/K2/K3 ablation | K1/K2 sometimes have lower mean RMSE, so K3 cannot be called uniformly best. | `K3 should be interpreted as the most conservative and certifiable predictor, not as the predictor with the lowest mean RMSE in every scenario.` | Protects the claim from being contradicted by the table. |
| B13 | P1 | Delay mechanism | `最新通信逻辑` is process wording and the mechanism should be link-level. | `The communication model treats disturbances as auditable link-level perturbations in relative distance, team lateral error, and upper-to-lower desired-path transmission, rather than as unstructured internal controller noise.` | Aligns the wording with the actual communication-aware controller. |
| B14 | P0 | FDI/FTC timeline | `reconfigured FTC MPC` suggests a separate optimizer, conflicting with Ch4's execution-layer FTC flag. | `The FTC reallocation flag then becomes active, producing nonzero steering/acceleration redistribution and changes in the vehicle-level control decomposition. The figure indicates that FDI/FTC and certificate monitoring are integrated into the closed loop, but it remains a single-run mechanism diagnostic.` | Keeps FDI/FTC consistent with the proof and avoids overclaiming from one run. |
| B15 | P1 | Mode-wise certificate evidence | Experimental certificate statistics may be read as proving global stability. | `The mode-wise statistics are consistent with the certificate-verified practical ISS/UUB conditions in Section 4, rather than proving global stability.` | Keeps experiment interpretation aligned with Ch4. |
| B16 | P1 | Main comparison and force peak | Force peak is higher than AKE in key scenarios, so it cannot be a primary superiority claim. | Table note: `Force-related quantities are reported as audit and trade-off metrics, not as the primary improvement claim against \AKE{}.` Interpretation: `The main comparison supports tracking and connection-utilization improvements; force-related quantities are interpreted as trade-off audits.` | Preserves the strongest evidence and avoids a likely reviewer objection. |
| B17 | P1 | Speed sensitivity | Paired 15 m/s remains a failure case, while the same-initial-condition run is diagnostic. | `At 15 m/s, the paired protocol remains a failure case for all methods. The same-initial-condition diagnostic is therefore reported separately to inspect whether updated low-level speed clipping, balanced progress supervision, and local lateral trimming can remove this failure mode in a representative run.` | Separates statistical evidence from diagnostic evidence. |
| B18 | P1 | Sine diagnostics | 15 m/s improvement is tiny and should not be framed as robust improvement. | `At 15 m/s, the improvement is marginal: lateral RMSE decreases only from $0.158565$ m to $0.157407$ m, and the pointwise win rate is 60.70\%. The 15 m/s result should therefore be read as a failure-mode diagnostic rather than robust performance evidence.` | Prevents over-positive high-speed wording. |
| B19 | P1 | Hairpin setup | `主证据` is too strong for same-initial-condition diagnostics. | `This subsection uses the same-initial-condition three-stage table as mechanism-oriented diagnostic evidence, while the paired statistics remain background evidence.` | Keeps the statistical hierarchy clear. |
| B20 | P1 | Hairpin interpretation | The full method improves connection utilization and force peak versus MethodHC, but loses some lateral tracking gain. | `The full method trades part of the lateral tracking gain for lower connection utilization and lower force peak; it should not be described as uniformly better on all metrics.` | Keeps the hairpin conclusion defensible. |
| B21 | P1 | Figure and table captions | Many Ch5 captions are long, Chinese, and narrative. | Translate all captions to English and use this structure: `Experiment X: metric or diagnostic name. Panel/table content. Scenario and statistical level. What the figure/table is used to inspect.` Do not include `证明`, `最新`, `调参`, or future-work workflow notes in captions. | Ensures Ch5 is visually English-complete, not only paragraph-complete. |
| B22 | P1 | Core ablation | `内部审稿材料` and redundant-bar explanations are not manuscript language. | `Because the table already reports success, lateral/longitudinal error, connection utilization, and certificate statistics, additional bar plots are omitted from the main text to avoid redundant evidence.` | Gives a clean reason for table-only presentation. |
| B23 | P1 | Certificate statistics table | Current wording should be more objective about positive and negative margins. | `High-communication-noise and mixed fault/noise show positive aggregate margins and certificate ok ratio $1.0$, whereas nominal clean and single fault contain negative worst-case margins.` Then link this to practical ISS/UUB, not global stability. | Avoids subjective phrases such as `表现较好`. |
| B24 | P0 | Ch6 and Ch7 | Separate `讨论` and `结论` are too short and repetitive. | Merge them into `\section{Discussion and Conclusion}` using the block in `B24 Replacement Block` below. | Produces a stronger ending with evidence, limitations, and future work. |
| B25 | P1 | Full English consistency | Tables may still contain Chinese column labels, row labels, and notes after paragraph replacement. | After applying text edits, run a table-label pass: translate table headers, row labels, minipage notes, and figure legends that remain in Chinese. Keep metric symbols and units unchanged. | Prevents a partially English Ch5 after the main paragraph/caption edits. |

## B05 Replacement Block: Ch5 Opening

```latex
\section{Experimental Results and Analysis}

This section reports eleven experiments and diagnostics corresponding to the method modules in Sections 3 and 4. \ZOHcons{} denotes a low-order zero-order-hold (ZOH) consistency baseline used to expose degradation without learned prediction or network-aware protection. The prescribed-performance-control (PPC) guard triggers constraint tightening or conservative control when the tracking error approaches the prescribed envelope. The ablation suffixes noComm, noDelay, noIRSP, and noPPC denote removal of communication-quality awareness, delay compensation, input-aware robust spectral projection, and the PPC guard, respectively.

Experiments 1--5 evaluate Koopman learning, prediction/projection, communication-delay mechanisms, FDI/FTC execution, and mode-wise certificate behavior. Experiments 6--11 report the main baseline comparison, speed sensitivity, same-initial-condition trajectory diagnostics, high-curvature hairpin behavior, module ablations, and certificate statistics. Experiments 6, 10, and 11 use paired $n=20$ statistics, Experiment 7 uses paired $n=3$ speed-stress statistics, and Experiments 2--5, 8, and 9 are single-seed or mode-wise diagnostics. These diagnostics inspect process-level behavior and are not treated as replacements for the paired statistical comparisons.
```

## B24 Replacement Block: Discussion and Conclusion

```latex
\section{Discussion and Conclusion}

In the mixed fault/noise and high-communication-noise cooperative-transport scenarios, \Method{} mainly improves tracking and connection-constraint utilization relative to the task-aligned \AKE{} baseline. The strongest paired evidence is that maximum connection utilization decreases from $0.2999$ to $0.0513$ under mixed fault/noise and from $0.3757$ to $0.0452$ under high communication noise, while lateral RMSE also decreases. The ablation results identify communication-aware consensus MPC as the dominant module: removing communication awareness raises connection utilization and degrades the certificate ok ratio under mixed fault/noise. These findings support the mechanism that link quality should enter consensus weighting, neighbor trust, and constraint tightening rather than remain a passive diagnostic signal.

The results also delimit the method. Force-related quantities are not uniformly better than \AKE{} and are therefore treated as trade-off audits rather than primary improvement claims. In the hairpin diagnostic, the high-curvature tracking branch substantially reduces lateral error but increases the force peak; the full payload-protection branch then lowers connection utilization and force peak at the cost of some lateral-error recovery. The speed study shows that longitudinal phase error becomes dominant at higher reference speeds, especially beyond 10 m/s. Same-initial-condition single-seed diagnostics are used only to inspect mechanism-level behavior and are not merged with the paired statistics. The stability result is certificate-verified practical boundedness under the logged certificate conditions, not global asymptotic stability.

This paper proposed \Method{}, a network-resilient cooperative-transport framework that integrates an input-aware stability-projected bilinear Koopman predictor, communication-quality-aware consensus MPC, adjacent-distance regulation, upper-layer 4WS local-path publication, upper-to-lower path communication, FDI/FTC execution-layer protection, and a Lyapunov/ISS certificate. The supported claim is network-aware cooperative transport with better tracking and connection control under degradation, not uniform improvement on every force or high-speed metric. Future work should rerun the updated diagnostic controller settings with multi-seed statistics, improve longitudinal phase control at high speed, and validate the payload-force proxy with richer load measurements or physical experiments.
```

## Recommended Selection Sets

| Set | IDs | Use Case |
|---|---|---|
| Minimum safe set | `B01`--`B03`, `B05`--`B08`, `B14`--`B20`, `B23`--`B24` | Fixes the most serious proof, language, statistical-boundary, force-tradeoff, and ending-structure risks. |
| Full balanced set | `B01`--`B25` | Recommended if the next step is to produce a coherent English Ch4--Ch7 manuscript version. |
| Language-only set | `B04`, `B08`--`B09`, `B21`, `B25` | Use only if the user wants visible English consistency before deeper technical edits. |
| Technical-claim set | `B01`--`B03`, `B11`--`B20`, `B22`--`B23` | Use if the user wants only claim-boundary and evidence-interpretation fixes. |

## Implementation Note

If the full balanced set is selected, apply the edits to a new dated `.tex` derivative, compile with XeLaTeX/BibTeX, and check for undefined references, overfull boxes, Chinese remnants in Ch5--Ch7, and table/caption overflow.
