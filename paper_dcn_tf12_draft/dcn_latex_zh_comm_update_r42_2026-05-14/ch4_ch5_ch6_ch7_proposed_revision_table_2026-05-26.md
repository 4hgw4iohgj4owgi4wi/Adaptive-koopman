# Ch4--Ch7 Proposed Revision Table for Review

Target source: `manuscript_en_ch4_reviewfix_2026_05_26.tex`

Review source: `ch4_ch5_ch6_ch7_review_2026-05-26.md`

Scope: Chapter 4 stability certificate, Chapter 5 experiments, Chapter 6 discussion, Chapter 7 conclusion.

Status: proposal only. No `.tex` manuscript text has been changed.

## Selection Guide

Use the `ID` column to tell Codex which changes to apply. Recommended minimum set: `C4-01`--`C4-03`, `C5-01`--`C5-07`, `C5-10`, `C5-14`, `C5-18`, `C6C7-01`--`C6C7-03`.

| ID | Priority | Location | Current Text | Proposed Replacement | Rationale |
|---|---:|---|---|---|---|
| C4-01 | P1 | Ch4 theorem, current lines 621--625 | `Let the state-dependent residual gain be small enough to be absorbed by the certificate decrease, i.e.,`<br><br>`\alpha_c:=\alpha-c_w\bar w_\chi^2L_\chi^2>0 .` | `Let $\alpha_{\mathrm{cert}}>0$ denote the nominal certificate decrease rate before absorbing the state-dependent residual term. Assume that the state-dependent residual gain is small enough to satisfy`<br><br>`\alpha_c:=\alpha_{\mathrm{cert}}-c_w\bar w_\chi^2L_\chi^2>0 .` | Defines the previously implicit `\alpha` and avoids a symbol appearing only inside the small-gain condition. |
| C4-02 | P1 | Ch4 after current lines 597--598 | `The mode certificates are assumed mutually comparable on the compact operating set: there exist $\rho_P\ge1$ and $c_P\ge0$ such that`<br>`$\bar V_{\nu,k}\le \rho_P\bar V_{\mu,k}+c_P$ for any modes $\mu,\nu$.` | Keep the current sentence, then add:<br><br>`The failure-growth constants used below include the worst-case jump induced by these mode-comparison factors.` | Makes clear that `\rho_P,c_P` are not forgotten when proving average contraction. |
| C4-03 | P1 | Ch4 after current lines 616--622 | `Assume that the input-aware robust spectral projection (IRSP) satisfies the strict sampled contraction bound ... and, when the norm certificate is enabled, ...` | Keep the current theorem text, then add after the norm-certificate sentence:<br><br>`Without this norm certificate, the boundedness claim is restricted to the sampled control envelope $\mathcal U_s$ used in the controller audit.` | Clarifies sampled IRSP versus continuous input-box coverage. |
| C4-04 | P2 | Ch4 proof, current lines 672--674 | `Mode changes are handled by the mutual comparability of the certificates through $\rho_P$ and $c_P$.` | `Mode changes are handled by the mutual comparability of the certificates through $\rho_P$ and $c_P$; their worst-case contribution is included in the failure-growth constants $\rho_f$ and $c_f$.` | Mirrors C4-02 inside the proof, closing the reader-facing logic. |
| C5-01 | P0 | Ch5 section title, current line 692 | `\section{实验结果与分析}` | `\section{Experimental Results and Analysis}` | The file is an English manuscript; the section title should be English. |
| C5-02 | P0 | Ch5 subsection title, current line 694 | `\subsection{实验组与基线协议}` | `\subsection{Experiment Groups and Baseline Protocol}` | Keeps chapter language consistent. |
| C5-03 | P0 | Ch5 opening paragraph, current line 696 | `本文使用十一组实验与诊断支撑方法模块。本文中 \ZOHcons{} 指低阶非 Koopman 一致性替代基线，只用于暴露无学习预测和无网络质量感知保护时的退化风险。PPC guard 指预设性能控制保护项（prescribed-performance-control guard），用于在误差接近预设包络边界时触发约束收缩或保守控制。表内消融后缀统一为 noComm、noDelay、noIRSP 和 noPPC，分别表示移除通信质量感知、时延补偿、输入感知稳定投影和 PPC guard。按本文出现顺序，实验1为 Koopman 训练损失、控制窗口预测与输入感知稳定投影审计；实验2为当前 24 维观测/31 维升维双线性 Koopman、双线性无投影、AKE-style 线性自适应 Koopman 与 6 维原始线性 Koopman 的闭环消融；实验3为通信扰动、上/下层链路与时延补偿机制诊断；实验4为 FDI/FTC 与安全监测时间线；实验5为分模式 Lyapunov/ISS 证书闭合；实验6为主对比，比较 \AKE{}、\MethodBase{}、\Method{} 和 \ZOHcons{}；实验7为同一压力场景下的速度敏感性；实验8为正弦路段同初始条件退化 \AKE{} 核验；实验9为 5 m/s 高曲率回头弯对比；实验10为核心模块消融表；实验11为 Lyapunov/ISS 证书统计表。实验6、实验10和实验11采用 paired $n=20$ 统计，实验7采用 paired $n=3$ 速度压力补充，实验2、实验3、实验4、实验5、实验8和实验9作为 single-seed 或分模式诊断补充。为避免主文被重复表格稀释，后续全模块消融和证书统计均以表格给出可追溯数值，不再重复插入同源柱状图。` | `This section reports eleven experiments and diagnostics that correspond to the method modules in Sections 3 and 4. \ZOHcons{} denotes a low-order zero-order-hold (ZOH) consistency baseline used to expose the degradation caused by removing learned prediction and network-aware protection. The prescribed-performance-control (PPC) guard triggers constraint tightening or conservative control when the tracking error approaches the prescribed envelope. The ablation suffixes noComm, noDelay, noIRSP, and noPPC denote removal of communication-quality awareness, delay compensation, input-aware robust spectral projection, and the PPC guard, respectively.`<br><br>`Experiments 1--5 evaluate Koopman learning, prediction/projection, communication-delay mechanisms, FDI/FTC execution, and mode-wise certificate behavior. Experiments 6--11 report the main baseline comparison, speed sensitivity, same-initial-condition trajectory diagnostics, high-curvature hairpin behavior, module ablations, and certificate statistics. Experiments 6, 10, and 11 use paired $n=20$ statistics, Experiment 7 uses paired $n=3$ speed-stress statistics, and Experiments 2--5, 8, and 9 are single-seed or mode-wise diagnostics. These diagnostics inspect process-level behavior and are not treated as replacements for the paired statistical comparisons.` | Shortens a very dense paragraph, removes internal narration, and clearly separates statistical tables from diagnostics. |
| C5-04 | P0 | Data-source note, current line 697 | `\textbf{数据源说明：} 实验6、实验10和实验11的 paired $n=20$ 表格，以及实验7的 paired $n=3$ 表格沿用既有批量统计；表 \ref{tab:nooffset_degraded_sine}、表 \ref{tab:nooffset_degraded_hairpin} 及图 \ref{fig:nooffset_fault_low_speed_panel}、图 \ref{fig:nooffset_fault_high_speed_panel}、图 \ref{fig:nooffset_hairpin_5mps_panel} 使用最新同初始条件 single-seed 参数重新生成。其中 2/5/10 m/s 正弦、15 m/s 正弦和回头弯均使用最新同初始条件核心；15 m/s 额外采用速度裁剪修正和平衡进度监督，回头弯额外加入高曲率抗扰与货物保护切换器。由于二者统计层级不同，本文不把 single-seed 调参结果直接回填到 paired $n=20$/$n=3$ 表格；若后续要求所有统计表完全使用最新控制器参数，需要重新运行对应多 seed 批量实验。` | `The paired statistical tables and the same-initial-condition diagnostics are reported separately because they differ in seed count and controller configuration. The single-seed diagnostics are used only to inspect trajectory-level behavior and are not merged into the paired statistics. A full multi-seed evaluation of the updated diagnostic controller settings is left for future work.` | Removes `最新`, `调参`, `回填`, and project-process wording while preserving the statistical boundary. |
| C5-05 | P1 | Baseline protocol, current line 699 | `基线协议包括两类对照。第一，\AKE{} 的来源是 Singh 等的自适应 Koopman 嵌入方法\cite{singh2025adaptiveKoopman}，本文将其离线 Koopman 表示学习、在线预测误差修正和 Koopman-MPC 控制思想迁移到四车刚性载荷运输任务，形成任务对齐的 \AKE{} 机制基线，用于当前场景下的同场对比。第二，\ZOHcons{} 是低阶非 Koopman 零阶保持一致性地板基线，用于呈现无学习预测/无网络感知保护时的退化风险。` | `Two types of baselines are used. First, \AKE{} is a task-aligned mechanism baseline derived from the adaptive Koopman embedding idea of Singh et al.~\cite{singh2025adaptiveKoopman}. It transfers offline Koopman representation learning, online prediction-error correction, and Koopman-MPC control to the four-vehicle rigid-payload transport task. Second, \ZOHcons{} is a low-order ZOH consistency baseline used to expose the degradation that occurs without learned prediction or network-aware protection.` | Replaces `地板基线` with a standard baseline description and avoids overstating a one-to-one reproduction of the AKE paper. |
| C5-06 | P0 | Ch5 subsection title, current line 701 | `\subsection{Koopman、时延与 FDI/FTC 机制证据}` | `\subsection{Koopman, Delay, and FDI/FTC Mechanism Evidence}` | English section consistency. |
| C5-07 | P1 | Fig. training caption, current line 706 | `\caption{实验1：Koopman lifting 网络训练/验证损失。含义：左图给出 total、prediction 和 lifted loss 在训练集与验证集上的收敛过程；右图给出最后 10 个 epoch 的训练/验证均值差距。该图用于证明网络训练过程可复查、可收敛且无明显发散。}` | `\caption{Experiment 1: training and validation losses of the Koopman lifting network. The left panel reports the total, prediction, and lifted losses on the training and validation sets. The right panel compares the average training and validation losses over the last 10 epochs. The figure checks convergence and excludes obvious divergence rather than serving as a formal proof of closed-loop stability.}` | Removes over-strong `证明`; turns it into an evidence/check statement. |
| C5-08 | P1 | Training-loss result paragraph, current lines 710--712 | `图 \ref{fig:e1_koopman_training_loss} 的客观结果是...` plus `造成上述现象的原因是...` | `Figure~\ref{fig:e1_koopman_training_loss} shows that the total, prediction, and lifted losses decrease on both the training and validation sets and enter a plateau after about 60 epochs. The validation losses over the last 10 epochs remain at the same order as the training losses, indicating no obvious sustained divergence or persistent train--validation separation.`<br><br>`This behavior is consistent with the offline data covering the main operating region in lateral error, curvature, and input excitation. The lifting network first learns a low-dimensional structure for reconstruction and one-step prediction, and the bilinear regression then fits the input-dependent evolution in the lifted space. Thus, the figure supports a trainable, projectable, and residual-auditable Koopman predictor.` | Replaces repeated `客观结果/造成原因` template with journal-style language and weakens causal wording. |
| C5-09 | P1 | Koopman prediction caption, current line 717 | `\caption{实验1：Koopman 预测与输入感知稳定投影审计。含义：...}` | `\caption{Experiment 1: Koopman prediction and input-aware stability-projection audit. The left panel reports one-step state RMSE for different lifted predictors. The middle panel shows 1--12 step rollout errors and 95\% confidence intervals, matching the MPC prediction horizon. The right panel reports the maximum, 95th percentile, and mean sampled spectral radius of $A_{\mathrm{eff}}(u)$ for the current 31-dimensional bilinear Koopman model over 96 normalized training-input samples before and after IRSP. The label stable\_bilinear denotes the stabilized bilinear predictor. The right panel shows that \eqref{eq:sampled_irsp}--\eqref{eq:norm_irsp_certificate} audit the input-dependent operator $A_{\mathrm{eff}}(u)$ rather than only the main matrix $A$.}` | English caption and clearer purpose. |
| C5-10 | P1 | Koopman prediction result, current lines 721--723 | `图 \ref{fig:e1_koopman_evidence} 的客观结果是...` and `造成上述结果的原因是...` | `Figure~\ref{fig:e1_koopman_evidence} shows that the bilinear model slightly improves one-step RMSE relative to the linear model, while the 95\% confidence intervals overlap at the main MPC horizon $H=12$. The main point is therefore not that the bilinear Koopman model uniformly minimizes open-loop rollout error. Rather, it provides comparable short-horizon prediction while IRSP reduces the sampled effective spectral radius from values above one to below one.`<br><br>`This is consistent with the bilinear structure capturing input--state coupling over short horizons, while open-loop rollouts still accumulate residual, normalization, and curvature-extrapolation errors. IRSP should therefore be interpreted as a controller-side amplification-risk audit, not as a prediction-accuracy enhancement module.` | Preserves the evidence boundary: prediction comparable, IRSP controls amplification risk. |
| C5-11 | P1 | Koopman ablation caption, current line 727 | `\caption{实验2：Koopman 设置闭环消融 single-seed 统计。含义：...}` | `\caption{Experiment 2: single-seed closed-loop ablation of Koopman settings. Under the same non-Koopman control modules, the table compares the current K3 predictor, the bilinear predictor without projection (K2), the AKE-style linear adaptive Koopman predictor (K1), and the raw 6D linear Koopman predictor (K0). It reports team lateral/longitudinal RMSE, the worst pointwise lateral-error gap relative to K3, and the pointwise win rate. A positive worst pointwise gap means that the corresponding variant has a larger absolute lateral error than K3 at at least one path sample.}` | English caption and clearer single-seed status. |
| C5-12 | P1 | Koopman ablation interpretation, current lines 757--759 | `表 \ref{tab:e9_koopman_dim} 和图 ... 的客观结果是...` and `造成上述结果的原因是...` | `Table~\ref{tab:e9_koopman_dim} and Fig.~\ref{fig:e9_koopman_dim_ablation} show that K0 raw6 linear Koopman is consistently worse in lateral RMSE and worst pointwise gap, whereas K1 and K2 can have lower mean lateral RMSE than K3 in some sine and hairpin cases. K3 should therefore be interpreted as the most conservative and certifiable predictor, not as the predictor with the lowest mean RMSE in every scenario.`<br><br>`The 24-dimensional observation includes payload-center, vehicle-local, connection-geometry, communication, and fault-context variables, and the 31-dimensional lifted state provides richer predictable error coordinates for MPC. In contrast, the raw6 linear model only propagates raw vehicle states and cannot represent curvature, connection geometry, and network disturbance coupling as effectively. The key role of K3 is to retain comparable prediction performance while bounding input-dependent amplification risk through IRSP.` | Avoids claiming K3 is uniformly best; aligns with actual table values. |
| C5-13 | P1 | Delay mechanism text, current lines 768--770 | `造成上述结果的原因是，最新通信逻辑把扰动从“控制器内部噪声”改为...` | `Figure~\ref{fig:delay_mechanism_evidence} shows nonzero delay and packet loss in both adjacent relative-information channels and upper-to-lower desired-path communication. The controller records relative-distance error, team lateral-error channel error, and upper-layer command bias, while activating 4WS local paths, lateral-priority weighting, and communication-aware constraint tightening.`<br><br>`The communication model treats disturbances as auditable link-level perturbations in relative distance, team lateral error, and upper-to-lower desired-path transmission, rather than as unstructured internal controller noise. As a result, delay compensation affects not only neighbor prediction but also link-quality weights, local-path preview, and constraint tightening.` | Removes `最新` and turns the paragraph into a manuscript-ready mechanism statement. |
| C5-14 | P0 | FDI/FTC result, current lines 779--781 | `随后全局控制模式进入 reconfigured FTC MPC...` and `该图证明 FDI/FTC 与证书监测已经接入闭环...` | `Figure~\ref{fig:fdi_ftc_timeline} shows that, after fault activation in the mixed fault/noise run, the controller-side fault flags and fault gaps increase together. The FTC reallocation flag then becomes active, producing nonzero steering/acceleration redistribution and changes in the vehicle-level control decomposition. The certificate function and margin continue to be logged during this transition.`<br><br>`This behavior is consistent with actuator-efficiency loss changing the closed-loop consistency between commanded and realized inputs. The FTC module compensates for the degraded vehicle through input redistribution inside the conservative feasible set, limiting the growth of connection error. The figure indicates that FDI/FTC and certificate monitoring are integrated into the closed loop, but it remains a single-run mechanism diagnostic.` | Aligns with Ch4: FTC is an execution-layer flag, not a separate optimizer; removes `证明`. |
| C5-15 | P1 | Mode-wise certificate caption and text, current lines 786, 790--792 | `分模式统计均为正裕度，说明压力场景下证书与保护逻辑一致...` and `因此本文稳定性表述应从...` | Caption replacement:<br>`\caption{Experiment 5: mode-wise Lyapunov/ISS certificate closure. The figure separates nominal and FTC-flagged steps in high-communication-noise and mixed fault/noise runs, and reports the certificate ok ratio, minimum margin, and 95\% practical bound. The mode-wise statistics are used to check consistency with the certificate-verified practical ISS/UUB conditions in Section 4, while avoiding an over-strong conclusion from aggregate worst-case margins.}`<br><br>Text replacement:<br>`Figure~\ref{fig:modewise_certificate_closure} shows that the nominal steps under high communication noise, the nominal steps under mixed fault/noise, and the FTC-flagged steps all have ok ratio $1.0$ and positive minimum margins. By contrast, the aggregate worst-case margins in Table~\ref{tab:e3_certificate} are negative for nominal clean and single-fault cases. This supports the certificate-verified practical ISS/UUB interpretation in Section 4, rather than a claim of global stability in all scenarios.` | Avoids reading experiments as mathematical proof and matches the revised Ch4 theorem. |
| C5-16 | P0 | Ch5 subsection title, current line 794 | `\subsection{主对比结果}` | `\subsection{Main Comparison Results}` | English section consistency. |
| C5-17 | P1 | Main comparison caption/table notes, current lines 798, 820, 825 | Chinese caption and `表内解读` block. | Caption replacement:<br>`\caption{Experiment 6: paired $n=20$ main comparison under mixed fault/noise and high-communication-noise scenarios. The table compares the proposed method, task-aligned Koopman baselines, and a low-order auxiliary baseline in terms of success rate, full-path success, tracking error, connection-constraint utilization, and force peak.}`<br><br>Table note replacement:<br>`\item Note: a larger force peak indicates a higher force cost. Force-related quantities are reported as audit and trade-off metrics, not as the primary improvement claim against \AKE{}.`<br><br>Minipage replacement:<br>`\textbf{Interpretation:} \AKE{} and \Method{} both complete the two pressure scenarios. The main differences are tracking error and connection-constraint utilization. Force peaks are reported in parallel as force-cost audits and are interpreted together with the hairpin diagnostics.` | Makes force boundary explicit and removes Chinese table note. |
| C5-18 | P1 | Main comparison interpretation, current lines 829--831 | `表 \ref{tab:e0_main} 的客观结果是...` and `造成上述结果的原因是...` | `Table~\ref{tab:e0_main} shows that \AKE{} and \Method{} both achieve full-path success under mixed fault/noise and high communication noise; success rate is therefore not the discriminating metric. The main differences are tracking error and connection-constraint utilization. Under mixed fault/noise, \Method{} reduces lateral RMSE from $0.0453$ to $0.0408$ and maximum connection utilization from $0.2999$ to $0.0513$ relative to \AKE{}. Under high communication noise, lateral RMSE decreases from $0.0519$ to $0.0424$ and maximum connection utilization decreases from $0.3757$ to $0.0452$. The force peak of \Method{} is higher than that of \AKE{} in both scenarios.`<br><br>`These results indicate that link-quality weighting, connection-geometry constraints, and Koopman-MPC prediction mainly improve tracking and connection utilization. The higher force peaks show that the current weights can use stronger transient control actions to preserve the connection geometry; force-related quantities are therefore treated as trade-off audits rather than primary superiority claims.` | Preserves the strongest evidence and explicitly avoids force overclaiming. |
| C5-19 | P0 | Ch5 subsection title, current line 833 | `\subsection{速度敏感性与正弦路段同初始条件核验}` | `\subsection{Speed Sensitivity and Same-Initial-Condition Sine Diagnostics}` | English section consistency and clarifies diagnostics. |
| C5-20 | P1 | Speed sensitivity result, current lines 860--862 | Chinese speed-sensitivity paragraphs. | `Table~\ref{tab:speed_sensitivity} reports paired $n=3$ speed sensitivity under the same pressure scenario. At 2 m/s and 5 m/s, \Method{} and \MethodBase{} complete the full path and reduce team lateral and longitudinal RMSE relative to \AKE{}. At 10 m/s, all methods still complete the task, but longitudinal RMSE reaches about $6.91$ m, indicating a separation between lateral tracking and longitudinal phase behavior. At 15 m/s, the paired protocol remains a failure case for all methods. The same-initial-condition diagnostic below is therefore reported separately to inspect whether updated low-level speed clipping, balanced progress supervision, and local lateral trimming can remove this failure mode in a representative run.`<br><br>`The sensitivity is consistent with increased reference speed amplifying yaw-rate demand, longitudinal phase error, and delay extrapolation. The communication-aware weights and connection constraints mainly suppress lateral and connection risks; after 10 m/s, longitudinal phase error becomes the dominant limitation.` | Keeps the 15 m/s paired failure clear and prevents conflict with the single-seed diagnostic. |
| C5-21 | P1 | Sine diagnostic table caption, current line 866 | `\caption{实验8：正弦故障/干扰路段同初始条件退化 AKE-M 核验。场景：...}` | `\caption{Experiment 8: same-initial-condition degraded AKE-M diagnostics on sine fault/disturbance paths. The 2/5/10/15 m/s sine-reference cases use the same seed, initial error, and spatial fault trigger for \AKEdeg{} and \Method{}. The table reports team-center lateral-error RMSE, maximum pointwise gap, and pointwise win rate for the panels in Figs.~\ref{fig:nooffset_fault_low_speed_panel} and \ref{fig:nooffset_fault_high_speed_panel}. The maximum pointwise gap is defined as $\max_t(|e_y^{\mathrm{NR}}(t)|-|e_y^{\mathrm{AKEdeg}}(t)|)$; a nonpositive value means that \Method{} is pointwise no worse over the whole trajectory.}` | English caption and reinforces diagnostic status. |
| C5-22 | P1 | Sine figure captions, current lines 891 and 905 | Two long Chinese captions beginning `实验8：2 m/s 与 5 m/s...` and `实验8：10 m/s 与 15 m/s...` | Low-speed caption replacement:<br>`\caption{Experiment 8: same-initial-condition degraded AKE-M diagnostics at 2 m/s and 5 m/s. The top row shows team-center $x$--$y$ trajectories, the middle row shows team lateral and longitudinal errors, and the lower diagnostic panels show vehicle-wise lateral error $e_{y,i}$, actual front steering angle $\delta_{f,i}$, longitudinal speed $v_{x,i}$, and longitudinal acceleration input $a_{x,i}$. Colors denote vehicles v1--v4; dashed lines denote AKE-M and solid lines denote \Method{}.}`<br><br>High-speed caption replacement:<br>`\caption{Experiment 8: same-initial-condition degraded AKE-M diagnostics at 10 m/s and 15 m/s. The panel layout is the same as in Fig.~\ref{fig:nooffset_fault_low_speed_panel}. The 15 m/s panel uses the speed-clipped balanced-progress diagnostic controller and is used to inspect whether the representative high-speed run remains within interpretable steering, speed, and acceleration ranges.}` | Shortens captions and removes process-heavy wording. |
| C5-23 | P1 | Sine diagnostic interpretation, current lines 909--911 | Long paragraph including `修正速度裁剪后的平衡进度监督核心...` | `Table~\ref{tab:nooffset_degraded_sine} and Figs.~\ref{fig:nooffset_fault_low_speed_panel}--\ref{fig:nooffset_fault_high_speed_panel} show that both methods start from zero lateral offset and use the same spatial fault trigger. At 5 m/s, \Method{} reduces team-center lateral RMSE from $0.044080$ m to $0.029711$ m, with zero maximum pointwise gap and a 100\% pointwise win rate. At 2 m/s and 10 m/s, the average RMSE also decreases, but local pointwise gaps remain. At 15 m/s, the improvement is marginal: lateral RMSE decreases only from $0.158565$ m to $0.157407$ m, and the pointwise win rate is 60.70\%. The 15 m/s result should therefore be read as a failure-mode diagnostic rather than robust performance evidence.`<br><br>`The pattern is consistent with network-aware prediction, lateral-priority weighting, and local-path/role scheduling suppressing lateral phase error most clearly at 5 m/s. At 2 m/s and 10 m/s, local steering phase, preview length, and longitudinal progress are not fully coordinated. At 15 m/s, low-level speed clipping and progress supervision dominate the behavior, so the diagnostic only shows that the representative run can be made complete with a very small RMSE reduction and a remaining local gap.` | Reduces over-positive framing for 15 m/s and removes internal tuning details. |
| C5-24 | P0 | Ch5 subsection title, current line 913 | `\subsection{高曲率回头弯对比}` | `\subsection{High-Curvature Hairpin Comparison}` | English section consistency. |
| C5-25 | P1 | Hairpin setup, current lines 915--917 | `本节保留同初始条件三阶段核验表作为主证据...` and `后续同初始条件核验...` | `The original paired $n=3$ hairpin stress statistics show that \AKE{}, \MethodBase{}, and \Method{} complete the full path. \Method{} reduces lateral and longitudinal RMSE and improves the certificate pass ratio relative to \AKE{}, but its payload-force RMS remains higher. To avoid duplicate hairpin tables, this subsection uses the same-initial-condition three-stage table as mechanism-oriented diagnostic evidence, while the paired statistics remain background evidence.`<br><br>`The hairpin scenario amplifies curvature tracking, longitudinal phase progression, and four-vehicle connection geometry. Therefore, the diagnostic compares \AKEdeg{}, \MethodHC{}, and \Method{} to separate high-curvature disturbance rejection from payload-force protection.` | Replaces `主证据` with mechanism diagnostic and removes project-flow phrasing. |
| C5-26 | P1 | Hairpin table caption, current line 921 | `\caption{实验9：5 m/s 回头弯同初始条件三阶段核验。场景：...}` | `\caption{Experiment 9: same-initial-condition three-stage diagnostic on the 5 m/s hairpin. The scenario uses a 5 m/s hairpin reference path, spatially triggered communication/fault disturbances, and variable 5--10 step upper-to-lower delay. The table compares \AKEdeg{}, \MethodHC{}, and \Method{} to distinguish high-curvature disturbance rejection from payload-resultant-force protection.}` | English caption and clearer purpose. |
| C5-27 | P1 | Hairpin figure caption, current line 938 | Long Chinese caption beginning `实验9：5 m/s 回头弯故障/可变高延迟路段...` | `\caption{Experiment 9: same-initial-condition three-stage diagnostic on the 5 m/s hairpin with fault and variable high delay. The top row shows team-center $x$--$y$ trajectories, the middle row shows team lateral and longitudinal errors, and the lower diagnostic panels report vehicle-wise lateral error, front steering angle, longitudinal speed, longitudinal acceleration input, planar payload resultant force $\sqrt{F_x^2+F_y^2}$, and the components $|F_x|$ and $|F_y|$. Dashed, starred, and solid curves denote \AKEdeg{}, \MethodHC{}, and \Method{}, respectively. The panel first inspects the tracking benefit of \MethodHC{} under high curvature and 5--10 step variable delay, and then checks whether the payload-protection switch in \Method{} reduces the force peak while keeping tracking errors interpretable.}` | English caption; keeps the two-stage mechanism explanation. |
| C5-28 | P1 | Hairpin interpretation, current lines 942--944 | Chinese result/trade-off paragraphs. | `Table~\ref{tab:nooffset_degraded_hairpin} and Fig.~\ref{fig:nooffset_hairpin_5mps_panel} show two effects. First, \MethodHC{} reduces team-center lateral RMSE from $0.298869$ m to $0.085119$ m and maximum lateral error from $0.587425$ m to $0.180423$ m relative to \AKEdeg{}, supporting the role of the 4WS local path, lateral-priority Koopman-MPC, and delay compensation in high-curvature disturbance rejection. Second, this tracking-priority variant increases the planar payload resultant-force peak to $15487.33$ N, compared with $5544.45$ N for \AKEdeg{}, indicating that aggressive lateral correction transfers part of the cost to the payload-force channel.`<br><br>`After activating the payload-protection switch, \Method{} reduces maximum connection utilization from $0.778074$ to $0.403050$ and the force peak from $15487.33$ N to $8874.04$ N relative to \MethodHC{}, while longitudinal RMSE decreases from $8.071913$ m to $6.341244$ m. The trade-off is that lateral RMSE increases from $0.085119$ m to $0.264159$ m. Thus, the full method trades part of the lateral tracking gain for lower connection utilization and lower force peak; it should not be described as uniformly better on all metrics.` | Keeps the safe force trade-off framing. |
| C5-29 | P0 | Ch5 subsection title, current line 946 | `\subsection{核心模块消融}` | `\subsection{Core Module Ablation}` | English section consistency. |
| C5-30 | P1 | Ablation caption and interpretation, current lines 950, 974--977 | Chinese caption and paragraphs including `内部审稿材料`. | Caption replacement:<br>`\caption{Experiment 10: paired $n=20$ core-module ablation on the sine path. The mixed fault/noise scenario combines communication noise with actuator degradation, whereas the high-communication-noise scenario imposes strong communication degradation only. The table quantifies the contributions of communication awareness, delay compensation, IRSP, the PPC guard, and \ZOHcons{} to task success, tracking error, connection utilization, and certificate ok ratio.}`<br><br>Text replacement:<br>`Table~\ref{tab:e2_ablation} shows that removing communication awareness produces the largest degradation. In mixed fault/noise, maximum connection utilization increases from $0.0550$ to $0.4294$ and the mean certificate ok ratio decreases from $0.99995$ to $0.78555$. In high communication noise, maximum connection utilization increases from $0.0446$ to $0.4384$ and longitudinal RMSE increases from $0.4709$ to $0.5399$. Removing the PPC guard makes the mixed fault/noise case fail the full path, while \ZOHcons{} fails in both pressure scenarios.`<br><br>`These results indicate that link quality is not merely a diagnostic variable; it directly changes consensus weights, neighbor-prediction trust, and constraint tightening. Because the table already reports success, lateral/longitudinal error, connection utilization, and certificate statistics, additional bar plots are omitted from the main text to avoid redundant evidence.` | Removes internal-review wording and keeps the key ablation mechanism. |
| C5-31 | P0 | Ch5 subsection title, current line 979 | `\subsection{稳定证书统计}` | `\subsection{Stability-Certificate Statistics}` | English section consistency. |
| C5-32 | P1 | Certificate table caption and text, current lines 983, 998--1000 | Chinese caption and paragraphs. | Caption replacement:<br>`\caption{Experiment 11: paired $n=20$ certificate statistics on the 5 m/s sine path. The table compares nominal clean, high-communication-noise, single-fault, and mixed fault/noise scenarios. It reports the Lyapunov/ISS certificate ok ratio and minimum margin to distinguish logged practical boundedness from strict global stability.}`<br><br>Text replacement:<br>`Table~\ref{tab:e3_certificate} shows that high communication noise and mixed fault/noise have positive aggregate margins and certificate ok ratio $1.0$, whereas nominal clean and single fault contain negative worst-case margins. The mode-wise decomposition in Fig.~\ref{fig:modewise_certificate_closure} further shows positive margins for the nominal and FTC-flagged steps in the pressure scenarios.`<br><br>`The difference arises because an aggregate minimum margin is sensitive to short numerical transients, switching instants, and local violations in non-pressure cases. The manuscript therefore uses the certificate-verified practical ISS/UUB formulation under a compact operating region, finite-density certificate failures, and fallback feasibility, rather than claiming strict global stability.` | More objective and consistent with Ch4. |
| C6C7-01 | P0 | Ch6 and Ch7 titles, current lines 1004 and 1011 | `\section{讨论}` and `\section{结论}` | Replace both sections with one section title:<br><br>`\section{Discussion and Conclusion}` | Current Discussion and Conclusion are too short and repetitive. |
| C6C7-02 | P0 | Ch6 first paragraph, current line 1006 | Entire paragraph beginning `在四车载荷协同运输的 mixed fault/noise...` | `In the mixed fault/noise and high-communication-noise cooperative-transport scenarios, \Method{} mainly improves tracking and connection-constraint utilization relative to the task-aligned \AKE{} baseline. The strongest paired evidence is that maximum connection utilization decreases from $0.2999$ to $0.0513$ under mixed fault/noise and from $0.3757$ to $0.0452$ under high communication noise, while lateral RMSE also decreases. The ablation results identify communication-aware consensus MPC as the dominant module: removing communication awareness raises connection utilization and degrades the certificate ok ratio under mixed fault/noise. These findings support the mechanism that link quality should enter consensus weighting, neighbor trust, and constraint tightening rather than remain a passive diagnostic signal.` | Turns current discussion into English and removes repetition. |
| C6C7-03 | P0 | Ch6 second paragraph, current line 1008 | Entire paragraph beginning `速度和回头弯实验进一步说明...` | `The results also delimit the method. Force-related quantities are not uniformly better than \AKE{} and are therefore treated as trade-off audits rather than primary improvement claims. In the hairpin diagnostic, the high-curvature tracking branch substantially reduces lateral error but increases the force peak; the full payload-protection branch then lowers connection utilization and force peak at the cost of some lateral-error recovery. The speed study shows that longitudinal phase error becomes dominant at higher reference speeds, especially beyond 10 m/s. Same-initial-condition single-seed diagnostics are used only to inspect mechanism-level behavior and are not merged with the paired statistics.` | Adds limitations/trade-off content needed for a real discussion. |
| C6C7-04 | P0 | Ch7 paragraph, current line 1013 | Entire conclusion paragraph beginning `本文提出了面向网络退化四车协同运输的 \Method{} 控制框架。` | `This paper proposed \Method{}, a network-resilient cooperative-transport framework that integrates an input-aware stability-projected bilinear Koopman predictor, communication-quality-aware consensus MPC, adjacent-distance regulation, upper-layer 4WS local-path publication, upper-to-lower path communication, FDI/FTC execution-layer protection, and a Lyapunov/ISS certificate. The paired multi-seed results support improved lateral tracking and connection-constraint control under communication and fault degradation, while the ablation results identify communication awareness as the main source of robustness. Mechanism-oriented diagnostics further examine Koopman training and prediction, IRSP, delay handling, FDI/FTC activation, mode-wise certificate margins, speed sensitivity, and high-curvature hairpin behavior.`<br><br>`The supported claim is therefore network-aware cooperative transport with better tracking and connection control under degradation, not uniform improvement on every force or high-speed metric. Future work should rerun the updated diagnostic controller settings with multi-seed statistics, improve longitudinal phase control at high speed, and validate the payload-force proxy with richer load measurements or physical experiments.` | Consolidates conclusion with limitations and future work, while avoiding overclaiming. |

## Compact Approval Checklist

| Apply? | IDs | Meaning |
|---|---|---|
| Recommended core | `C4-01`--`C4-04` | Small proof-closure edits only. |
| Recommended English cleanup | `C5-01`--`C5-06`, `C5-16`, `C5-19`, `C5-24`, `C5-29`, `C5-31` | Translate section/subsection headings and opening protocol. |
| Recommended evidence-bound cleanup | `C5-07`--`C5-15`, `C5-17`--`C5-18`, `C5-20`--`C5-32` | Replace internal-review phrasing and over-strong claims. |
| Recommended structure change | `C6C7-01`--`C6C7-04` | Merge Discussion and Conclusion into one stronger closing section. |
| Optional minimal path | `C4-01`--`C4-04`, `C5-03`--`C5-05`, `C5-14`, `C5-18`, `C5-23`, `C5-28`, `C6C7-01`--`C6C7-04` | Handles the largest reviewer risks without translating every caption. |

## Notes Before Implementation

1. Applying all rows will make Chapters 4--7 more consistent as an English manuscript, but Chapter 5 tables still contain Chinese column labels unless separately selected for table-label translation.
2. If the user wants a fully English Ch5, table headers and row labels should also be translated in a second pass.
3. After any selected changes are applied, the manuscript should be compiled with XeLaTeX/BibTeX and checked for undefined references/citations and layout issues.
