# DP-MHK协议审计修订A2

- 修订时间：2026-08-22；发生在任何pilot或正式D4R2生成之前。
- D0初版扫描器错误地把JSON中SHA256、浮点小数和文件大小的任意六位数字当作seed，先后误报`161152`、`164103`和`164173`。
- 修复后只解析键名含`seed`、`traj_id`或`trajectory_id`的结构化字段。
- 结构化扫描确认原预注册号段train `161001–161192`、validation `162001–162064`、development `163001–163064`的真实碰撞为0，因此撤销临时A1换段，恢复原号段。
- 两次D0失败和临时A1均保留在工作记录/solutions中；没有生成任何实验数据。
- 方法、工况表、样本量、门槛和统计规则未改变。

## D2追加表冻结

- 原320条生成后的唯一覆盖缺口为train高载荷窗`107<200`；其余工况、反转、ultimate和合同门均通过。
- 按原任务书“每组增加8条、最多两轮”冻结：round1 seed `167001–167032`，round2 seed `168001–168032`；两段结构化seed碰撞均为0。
- G0保持直线/低载荷，G1保持加载；G2/G3复用已通过安全pilot的`speed=4 m/s, front=15°, rear=-1, diagonal`，G2 hold、G3 reversal。
- 每轮整组32条；达到`>=200`后不启动下一轮。0.8 rated门、窗口stride和其他阈值不变。
