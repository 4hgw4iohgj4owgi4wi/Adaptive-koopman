# Koopman组合改进普适性实验 v2 执行修正

> 日期：2026-08-22  
> 基础协议：`koopman_universal.md`，冻结SHA256 `DF6EE9810137687B9C3814040FFF827DB1DC2DFAE1ED2F2045E6F2618F69BCF7`  
> 修正原因：v1安全扫描通过，但D1-train的L3覆盖为`927/1000`，差73个互不重叠20步窗。用户明确授权修改可调整卡口并完成全部实验。  
> 原则：保留v1失败，不降低1000窗门槛、不重分已有轨迹、不删除失败seed；把v1 D1视为独立pilot，新增冻结seed池后建立v2数据版本。

## 1. 数据覆盖修正

保留v1全部368条D1轨迹及hash，新增36条强受力轨迹：

- train：E2/E3/E4/E5/E6/E9各4条，共24条；seed为`919000 + 100*scene_index + {0,1,2,3}`；
- validation：上述六场景各1条，共6条；seed为`919800 + scene_index`；
- development-test：上述六场景各1条，共6条；seed为`919900 + scene_index`；
- 场景索引沿用E0–E6/E9=`0,...,7`。

新增轨迹只从v1安全扫描中选择峰值位于50–80%额定力、且L3窗最多的合法配置。每个split内按场景交替使用hold/reversal；参数继续用原内部范围和对应新seed抽样。生成前冻结全部seed、配置、代码和v1 manifest hash。

v2 G1仍要求train每个L0–L3及R1/R3/R4至少1000窗，validation/development-test各至少200窗。额外以train L3达到1200窗作为设计余量目标；`1000–1199`虽可通过原统计门，但必须标为边缘覆盖。不能为了达到1200继续逐条追加seed。

## 2. 卡口执行方式修正

性能和安全阈值不降低，但把“全局停止”改为“分支淘汰”，以完成全部可解释实验：

1. G2中秩低于90%、条件数大于`1e8`、非有限或发散的骨干只退出F/H主分母，仍保留诊断；其余骨干继续。
2. F若未达到跨3家族，不再阻断H；F结论记为不普适，H继续在所有G2合格骨干上独立运行。
3. BFH若未达到受限普适，T/C/P7仍遵守前置机制门；A作为独立参数适应实验继续运行，不与BFH联合授予优势。
4. T/C没有潜空间漂移证据时记`not_applicable_no_mechanism`，不训练该模块，但不阻断A、D2或报告。
5. D2对所有已冻结且数值有限的P0–P6候选一次性确认，不因development结果不佳而只挑好模型。
6. 闭环分为两层：通过绝对部署门的候选进入正式闭环；其他数值有限候选进入相同工况的诊断闭环并明确标记`diagnostic_only_offline_gate_failed`。诊断闭环不能形成部署主张。
7. 单个候选在闭环非有限时仅停止该候选，其余候选与K0/K1基线继续。

## 3. 训练与确认冻结

- 新增模块训练seed仍为`92001–92005`；rank、ridge、RLS、T、C、A候选沿用v1。
- D2 seed和规模沿用v1；生成前冻结v2协议、v2 D1 manifest、所有模型和评估脚本hash。
- 原compare确认集和v1 D1均已查看，不得作为v2盲确认；D2只生成一次。
- 所有实现修复、失败分支、后验展示调整继续写入项目主`revision_2026/work_log.md`和`universal_v2/work_log.md`。

## 4. 完成定义

v2只有在以下事项均有明确状态后才算完成：D1-v2覆盖、B0–B5B审计、F、H-LS/H-RLS、T/C机制门、A zero/few/online、D2一次性确认、正式或诊断闭环、全部底层CSV/JSON、图片、最终优劣报告和工作记录。`not_applicable`与负结果属于完成状态，但不得伪装为通过。

## 5. G2卡口勘误（执行中冻结）

首次按v1原句执行G2后，八个骨干均因20步`divergence_rate>0`被排除，但全部骨干的`nonfinite_rate=0`。这里存在逻辑自锁：把“冻结骨干长时域误差超过50个归一化单位”当成进入H长时域修正前的数值前置失败，会禁止H回答其预注册问题。

本勘误不删除首次判定，原`backbone_audit.csv`和`t2/complete.json`继续保留；新增`gate_amendment.json`进行如下重分类：

1. F/H训练入口只检查回归矩阵条件数`<=1e8`、有效秩比例`>=0.95`和全程有限值；
2. `divergence_rate`仍按原50归一化单位阈值报告，并作为模块必须改善的性能结果，不再兼作模块入口；
3. 秩比例低于0.95、非有限或数量级爆炸的骨干仍只保留诊断，不得靠裁剪进入主分母；
4. H之后的候选仍必须重新通过状态保护、有限性、发散不增加和绝对部署门；本勘误不降低任何最终性能或安全阈值。

按首次审计的冻结数值，进入F/H的骨干为`K0、K1、K4、K5-linear`；`K2、K3、K3-r2、K5-bilinear`保留为失败边界。该分类在训练F/H前冻结，不能根据后续收益再变更。
