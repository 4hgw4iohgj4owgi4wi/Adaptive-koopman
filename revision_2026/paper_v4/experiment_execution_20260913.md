# EXP-R4执行记录：问题、处理与效果

> 2026-09-14 10:07续接：串行有力窗口完整完成并复现288.059667N峰值，50/500/50证据、源物理逐位一致、32ε时间合同及单条PNG/SVG图表均PASS。随后按同v3协议/共同截止启动唯一parallel8（PID23808）；完整1ms仍需等待配对比较与图表。

> 2026-09-14 09:32按最新第25节恢复执行：时间身份v2通过且旧FAIL保留；有力窗口选定为26.66—27.66s，覆盖首个全程最大点力288.059667N。QP严格等价及非线性门PASS，图表NaN仅以后处理修正且未重跑QP。串行有力窗口已启动（PID36904、截止10:32:20）；后续仍按串行→并行→比较→预算复核门推进。

> 2026-09-13 23:05停止裁决：PARALLEL01完整完成并与SERIAL02在全部物理/控制字段、子步、QP哈希和首控制上逐位一致；速度提升2.18247倍。正式分析因源切片边界及时间戳非逐位差异返回FAIL/exit 20。对齐后只剩最大7.1054e-15s的`time_s`差异，但任务书明确禁止自动容差接受，故不启动1ms/0.5ms，B3保持BLOCKED。问题、诊断和效果见`results/20260913_R4B_B2B_COMPARE01/failure_diagnosis.json`。

> 2026-09-13 22:31进展：SERIAL02通过完整性与运行证据核对（125周期、1250子步、125条solver、stderr为空，墙钟3922.795s），后台承载修复实际生效。已按同一协议与共同截止启动唯一PARALLEL01（PID37684）。尚未运行两窗逐位比较，因此B2b、B3和R3均不得写为PASS。

> 2026-09-13 21:22再次续接：用户“继续”后冻结唯一B2同窗重试预算；隐藏后台存活探针PASS。SERIAL02已从42.00s全新启动（PID17928，截止23:22:46），不拼接SERIAL01。启动复核进程存活、stderr为空，科学门不变。

> 2026-09-13 21:19续接：EXP-R4-B的B0/B1与B2a已PASS；B2b串行前台进程在45/125周期后被会话回收。45周期raw/substeps完整，第46条控制仅求解未施加。因原两小时截止不足以完整重跑且断点等价未验证，裁决为`B2B_INTERRUPTED_INCOMPLETE / B3_BLOCKED`。处理和证据见`results/20260913_R4B_B2B_SERIAL01/interruption.json`。

执行时间：2026-09-13 13:47—14:09（Asia/Shanghai）  
执行范围：`experiment.md`第20.5节U0—U3；依照停止门，未启动U4及其后依赖批次。

## 结论

- U0现场核对完成：主机为`DESKTOP-9IUUGEO`，Python 3.11.14；未发现正在运行的`paper_v4`、`pilot_runner`或R3实验进程；本地最新动力学产物仍为`20260911_R3_UNFROZEN_FULL01`。
- U1通过：原2 ms轨迹的候选、协议、runner、controller、batch和递归植物身份全部闭合；使用新候选身份后，独立物理/证据重审全部通过。该结论是`PASS_RECONSTRUCTED_IDENTITY`，不是把原`status.json`改写为PASS。
- U2通过诊断但预算门失败：四个登记QP点的矩阵逐位复现、QP求解和非线性复核均通过；新逐步雅可比候选平均每步33.559876 s，目标为不超过5 s。
- U3两轮等价加速均失败预算门：8进程为14.151784 s/步，16进程为14.684687 s/步。两轮的P/q/A/l/u/u_nom均与保存矩阵逐位相同，说明并行方案没有改变QP数学，但仍分别超预算183.04%和193.69%。
- 根据任务书“小工程错误最多修复两轮”及U3预算门，停止U4。没有启动1/0.5 ms完整路线，没有启动R4/R5，也没有产生新的算法优越性、实时性或Koopman/通信贡献结论。

## 问题、解决方法及实际效果

|问题|解决方法|实际效果|裁决|
|---|---|---|---|
|最新EXP-R4未注册，直接使用会被旧协议白名单拒绝|把当前任务书冻结为`inputs/experiment_EXP-R4_97D79F76.md`，登记父EXP-R3 SHA和EXP-R4 SHA；扩展CLI白名单并保留错配负例|EXP-R4 preflight运行成功；E00仍按事实为PARTIAL|协议身份闭合，不提升E00科学状态|
|`r2c_analyze.py`把旧runner SHA写死，导致新候选2 ms的`source_identity=false`|把预期runner SHA改为显式候选参数；增加`candidate_identities.json`和只读候选审计；旧EXP-R2与新EXP-R3候选分开验证|15项身份/证据检查全部为true；原2 ms独立物理重审PASS|U1通过，原FAILED文件原样保留|
|原运行没有单独保存完整运行时源码包|联合使用运行前递归plant清单、启动登记中的runner/controller/batch哈希、文件时间顺序和同哈希只读快照；补错误runner/controller/protocol/candidate/缺快照等负例|正例通过，5类负例均被拒绝|标记`PASS_RECONSTRUCTED_IDENTITY`；披露其不是签名式运行时包|
|不知道32.6 s主要消耗在哪个环节|在四个已登记QP点上分别计时逐步动力学线性化、特征雅可比、约束雅可比、QP求解和非线性复核；不传播闭环|两个逐步雅可比点的动力学线性化分别占QP构建约94.14%和94.95%；QP setup+solve约0.01 s，不是根因|停止收紧OSQP容差或更换求解器的无依据尝试|
|逐步有限差分串行计算过慢|保持公式和重构顺序不变，仅把每个雅可比内彼此独立的中心差分rollout分派到常驻进程；按8进程、16进程做两轮|相对33.56 s平均值，8进程单点加速约2.37倍，16进程约2.29倍；矩阵逐位一致，但均未达5 s|预算门失败，停止完整R3|
|首次pytest尝试出现根目录缓存权限警告|复验时禁用pytest cache并把临时目录放到`paper_v4`下|`3 passed in 0.82s`，无测试失败|工程验证通过；不等于完整mandatory suite已实现|

## 关键计时证据

|登记点|总计 s|QP构建 s|动力学线性化 s|QP求解 s|非线性复核 s|
|---|---:|---:|---:|---:|---:|
|42.58 s，冻结雅可比，FD×0.5|4.268|3.830|1.628|0.010|0.428|
|42.58 s，冻结雅可比，FD×2|4.159|3.797|1.632|0.010|0.352|
|42.58 s，逐步雅可比|34.819|34.343|32.327|0.010|0.466|
|43.06 s，逐步雅可比|32.301|31.923|30.311|0.010|0.368|

这些数字是本次本机复算的开发诊断，不替代2379步完整轨迹的既有平均值32.635780 s。

## 产物

- `results/20260913_E00_PREFLIGHT_R4_01`：EXP-R4预检，E00仍为PARTIAL。
- `results/20260913_E00_CLI_R4_TEST01`：CLI负向保护与代数反例。
- `results/20260913_U1_IDENTITY01/identity_reaudit.json`：原2 ms只读身份和物理证据重审。
- `results/20260913_U2_QP_COST01/qp_cost_profile.json`：四点成本归因。
- `results/20260913_U3_PARALLEL_EQ01/parallel_equivalence.json`：8进程等价性和预算结果。
- `results/20260913_U3_PARALLEL_EQ02/parallel_equivalence.json`：16进程等价性和预算结果。
- `protocol/candidate_identities.json`：版本化候选身份合同。
- `protocol/source_snapshots/EXP-R3-unfrozen-v1_postrun_verified`：只读保留的同哈希源码快照。

## 保持不变的边界

- `results/20260911_R3_UNFROZEN_FULL01/status.json`未修改，SHA256仍为`c1db7cde0e17f4dabb755c51403086db79dd2900b201dcdfa6904c00e4cac8f8`。
- 原2 ms完成事实和物理证据可恢复为有效开发证据，但计算预算仍独立失败，且1/0.5 ms同候选结果仍为NOT_RUN。
- 后续若继续，必须先提出新的有界计算方案或明确修订离线预算；不得直接串行启动两条各约22小时的路线，也不得放宽物理/位姿/请求约束换取速度。
