# EXP-R4-C执行记录：问题、处理与效果

执行入口：`experiment.md`第25节；执行前任务书SHA256为`ab76e57e4830a8deb926ed6e65a602b799db1b4d9e0d830106587564c89a6b03`。

## 已完成

- 冻结并执行新时间身份合同。v2协议SHA256=`77e98747ff01cd8e615e0e307fa406938463d641099044be60439c90958b50c0`，最终状态`PASS_NEW_TIME_IDENTITY_CONTRACT`。
- 三方raw、substeps、solver时间均唯一映射登记整数tick；最大表示误差7.105427357601002e-15s，低于事前32ε界。物理数组仍逐位比较，9项正负例全部通过。
- 旧`20260913_R4B_B2B_COMPARE01/window_equivalence.json`保持FAIL，没有覆盖或追认。
- 时间审计PNG/SVG及图表清单完成并经打开检查，figure_status=PASS。
- 在冻结2ms源中按固定规则登记首个最大点力窗口：最大288.0596665626036N，connector0，t=27.154s；窗口26.66—27.66s。
- 有力起点QP预检通过：P/q/A/l/u/u_nom和首控制逐位一致，串/并非线性验证PASS。串行构建36.444446s，并行构建13.940386s，继续标记`OFFLINE_ONLY / FAIL_ORIGINAL_5S_BUDGET`。
- 串行有力闭环完整完成：50周期、500个2ms子步、50条solver、stderr为空、墙钟1550.584120s；峰值点力288.0596665626036N。与冻结源的非时间物理量逐位一致，时间身份通过32ε合同。
- 串行单条六面板PNG/SVG、图表清单和中文README已完成，打开检查`figure_status=PASS`。
- 并行有力闭环完整完成：50周期、500子步、50条solver、stderr为空、墙钟702.142416s；相对串行加速2.208361倍。
- 严格配对状态`PASS_FORCE_WINDOW_EQUIVALENCE`：所有物理、子步、QP哈希、首控制及登记时间身份门通过；串行、并行和配对图表均PASS。
- 12h预算复核PASS：按14.04284832s/周期外推完整路线9.279982h，名义余量2.720018h；只释放完整1ms协议准备。

## 遇到的问题、处理和效果

1. 时间审计v1误要求历史源solver提供未记录的`problem_hashes`，程序在裁决前停止。处理：保留失败目录，v2明确该字段不可用，不伪造数据，同时维持实际可核验的严格门。效果：时间身份与物理身份分离验证通过。
2. 有力窗口协议v1/v2各有一个SHA手工转录错误，均被身份门在QP构造前拒绝。处理：保留两次失败证据，按两轮工程修复上限建立v3，并在执行前程序化核对全部12项身份。效果：零不匹配，未浪费QP或动力学预算。
3. QP首图对相同无穷边界计算`inf-inf`产生NaN，零差异面板空白。处理：保留原图，只从已保存QP数组重新后处理生成`force_qp_preflight_qa.png/.svg`。效果：六组差异0清晰可见，未重跑QP，科学裁决未改变。
4. 启动并行前的重复进程检查使用全命令行模糊匹配，误把正在执行检查的PowerShell自身识别为目标进程。处理：把筛选限定为Python进程并再次确认目标目录不存在。效果：排除假阳性后只启动一个parallel8进程，没有重复运行或覆盖输出。
5. 单条图脚本在无标签的构形误差面板调用legend，出现空图例框。处理：保留首图，仅修改后处理图例选择并生成`_qa`版。效果：串行/并行修正版无空框，打开检查通过，仿真和科学阈值不变。

## 当前门控

- 有力串行、并行、严格配对和对应图表已全部PASS。
- 预算复核后已冻结完整1ms协议（SHA=`14f4e010...d059dd`）：专用runner的21项身份正向预检和错误SHA负例通过。
- `R4C_FULL_1MS01`于10:25:50在DESKTOP-9IUUGEO启动，PID16796，parallel8、2379周期、1ms最大植物步、截止22:24:13；前4条solver/非线性复核PASS且stderr为空。
- 1ms未完整结束及独立审计/图表PASS前，0.5ms、R4、R5仍不得启动。

## 完整1ms结果

- 完成2379/2379周期、47.58s、参考95.1283155163m，墙钟33308.693095s（9.2524h），stderr为空，早于12h截止。
- 独立21项审计全PASS；峰值力288.059652N、轮胎利用率0.0499304、最小支承4677.138N、最大构形误差12.9976mm。
- 2379个求解全部超过原5s预算，均值13.72775s；单条物理/证据通过不等于实时或收敛通过。
- 绘图遇到两次仅后处理错误：状态枚举混用、缺少`src`导入路径。确认空目录后清理并修复，最终PNG/SVG打开验收PASS，未重跑仿真。
- 当前只释放0.5ms协议冻结/启动；完整R3继续依赖1/0.5ms严格比较。

## 完整0.5ms启动

- 协议SHA=`4e365ee5...d2a7fe`；21项身份、1ms审计及图表父门PASS，错误SHA负例拒绝且未建输出。
- 19:49:40启动唯一`R4C_FULL_0P5MS01`，PID32068，parallel8、2379周期、0.5ms最大植物步；截止9月15日07:48:23，实际预算11.9786h。
- 首条solver/非线性复核PASS、进程存活、stderr为空。完整R3比较、R4、R5仍未释放。

## 2026-09-15 完整0.5ms结束与R3配对裁决

- 05:09:40左右正常完成2379/2379周期、47.58s、参考95.1283155163m；墙钟33601.870952s（9.3339h），stderr为空，未触及12h截止。
- `single_run_audit.json`独立21项全PASS：峰值点力288.059651N、最大内部力180.164761N、轮胎利用率0.0499304、最小支承4677.138N、最大构形误差12.9976mm。单条六面板PNG/SVG打开QA PASS。
- 原5s计算预算仍失败：2379步全部超限，均值/P95/最大13.585393/13.937637/18.299252s；不得写实时控制。
- 只读比较器首试因NumPy布尔不能JSON序列化退出；输出目录经核实只有空`figures/`后删除。修复显式`bool`并把无效的空父字段比较替换为共同任务书父SHA和共享计算源码身份核对；未重跑动力学。
- 决定性1→0.5ms结果PASS：峰值最大相对差4.5892936e-8，冲量向量3.2463998e-6，终点位置8.7018595e-6m，航向4.1915693e-5°；均显著低于2%/2%/1mm/0.01°注册上限。
- 配对诊断图与四门图PNG/SVG全部打开QA PASS。当前`EXP_R4C_RECOVERY_PASS / R3_NUMERICAL_PAIR_PASS / ORIGINAL_5S_BUDGET_FAIL_RETAINED / R4_R5_NOT_STARTED`。
