# R3通过后的实验接续详细任务书

> 当前接续修订见experiment.md第31节：用户指定2200周期P1归为后期跟踪发散/未通过，旧外部中断事实保留，旧CPU不续跑或重跑。GPU按用户报告已搭好，先核对最新资格并处理P1晚段失效，再以新登记验证恢复R4依赖；下文按顺序直接排六条R4的计划暂缓，不覆盖此最新停止状态。

版本：POST-R3-DRAFT-20260915。当前为文档交付，未冻结为可运行协议，未启动仿真。作为experiment.md第27节的详细附件；与第0—26节冲突时，当前状态及本附件明确列出的接续修订优先，未修改的科学/信息/统计门继续沿原书执行。

## 1. 起点、目标与完成口径

起点为20260915_R4C_FULL_COMPARE01/r3_convergence.json的PASS：名义P0下，指定集中全状态物理MPC的完整1/0.5ms比较通过。两条各2379周期、47.58s、95.1283155163m参考，墙钟9.2524/9.3339h。峰值相对差4.5892936e-8、冲量相对差3.2463998e-6、终点位置8.7018595e-6m、航向4.1915693e-5°。单条与配对图已存在。

这不等于E01全部通过、E02预测资格通过或六方法正式比较完成。E00保持PARTIAL；原历史R3失败和严格时间B2b失败不覆盖。三种状态必须分别登记：execution_status（是否完整运行）、science_status（是否通过登记科学门）、figure_status（图表是否完成并QA）。

最终研究链条为：可信物理与合法信息 → 合格预测/优化器 → 预测和网络模块独立收益 → 强基线/统计/适用域 → 理论与论文证据。方法无收益允许得出负结论，不允许改门追求“全部成功”。

## 2. 接续任务树与依赖

| 编号 | 工作包 | 当前状态 | 前置 | 完成后释放 |
|---|---|---|---|---|
| C0 | 新旧runner/报告/协议身份迁移 | 待实施 | 当前R3证据 | R4前置资格 |
| C1 | R4两参数点×三步长 | 六条未运行 | C0、图表收尾和预算就绪 | R5及执行器诊断 |
| C2 | R5无噪声/基础噪声合法信息 | 两条未运行 | C1全PASS | E01/E03接口证据 |
| C3 | 执行器更新间隔诊断 | 九短片段未运行 | C1全PASS | E01离散语义证据 |
| C4 | 原E01逐项缺口核销 | 部分完成 | C1—C3及原要求 | 完整plant_gate |
| K | E02预测复现、闭合与资格 | 当前版本未完成 | E00相关数据门、C4 | 核心预测方法资格 |
| M | E03/E04优化、信息、架构 | 部分工程已有，正式资格未完成 | C4、合法信息接口 | 固定公平控制平台 |
| N | E05核心比较、E06/E07补偿消融 | 未完成 | K/M和正式数据资格 | 主贡献的成对证据 |
| B | E08强基线/E09网络压力 | 未完成 | 来源资格、M；正式比较须K/N相应冻结 | 外部竞争力和边界 |
| A | E10—E12观测/适应/故障/消融 | 未完成 | 对应模块接口及N/B必要分支 | 模块保留/删减决定 |
| S | E13/E14计算、敏感性、适用域 | 开发诊断部分已有 | 随开发推进；正式参数冻结前完成选参 | 工作域与代价 |
| V/T/W | E15独立验证、E16理论、E17论文 | 主要证据未闭合 | 各自资源/来源/方法定义 | 审稿逐条关闭 |

E00资料补证、E08原文资格审查、E16理论核对可独立推进，不需等待全部仿真结束；涉及数据数组和确认池的读取仍受角色门约束。上述可并行工作不表示同时启动多个长仿真或多智能体。

## 3. C0：先修接续接口，不回退已验证算法

当前已核实的风险：

| 文件/接口 | 已发现问题 | 必须完成的迁移 |
|---|---|---|
| src/paper_v4_core/r4_batch.py | 只接受旧scope及顶层lambda_internal，当前R3报告schema不同 | 版本化解析当前PASS报告并校验候选、双方协议及源哈希，禁止只检查status字符串 |
| 同文件调用pilot_runner.run | 未显式传frozen_dynamics_jacobian，旧run默认True | 明确使用已验证逐步雅可比False、FD尺度1、parallel8及相同控制定义，不能只换候选名 |
| r5_batch.py | 旧R4报告schema、--baseline-r2c指向可能过期的旧候选 | 明确同候选同参数同步长全状态基准，记录baseline run及SHA |
| r5_batch.py::_noiseless_difference | 直接比较全部同名列，可能包含solver_wall_s及浮点time_s | 白名单物理/控制字段严格比较；时间按已通过合同独立核对；墙钟只作成本指标 |
| r5_runner及合法信息solve入口 | 旧EXP-R2实现，当前逐步雅可比/并行身份尚未重新资格验证 | 逐层核查PilotConfig、模型参数、控制记忆和预测器，补身份回归，不假定默认值正确 |

交付：新接续协议schema、候选/实现/参数ID映射、字段比较白名单、源文件SHA清单、负例报告、命令--help记录、绘图收尾正负例。不得修改历史协议和旧run以迎合新解析器。

负例至少覆盖：旧冻结雅可比候选、错误父R3报告、缺候选字段、错误参数/步长、错协议SHA、未过figure门、重复输出目录、现有活动run。拒绝必须发生在动力学启动和创建正式run输出之前。正例用已保存QP状态复算，先证明确实加载已验证新候选。

参数改变必须同时明确“真实植物参数”和“预测器知道的参数”。C1是数值/物理覆盖诊断，采用同一登记参数对象供植物和预测模型；这不构成未知参数鲁棒性实验。若现代码二者不一致，停止登记并解释，不能悄悄改成失配试验。正式E14失配另行规定仅知道名义值及公开范围。

## 4. C1/R4：六条参数覆盖与数值验证

固定候选EXP-R3-unfrozen-v1，N=20、lambda_internal=2、frozen_dynamics_jacobian=False、FD尺度1、parallel8；控制20ms、执行器2ms保持，内部最大步长仅按表变化。路线沿现有冻结回头弯、2m/s、11.5m恒曲率核心及两端11m过渡；47.58s、2379周期、95.1283155163m参考。读取e01_routes.json和实际参数构造器逐项核对绝对值，登记质量/刚度/阻尼/摩擦等展开表，而不只保存比例。

| 顺序/单元 | 载荷比例 | 连接器比例 | 摩擦系数 | 最大植物步长 |
|---|---:|---:|---:|---:|
| R4-P1-2 | 0.9 | 0.95 | 0.95 | 2ms |
| R4-P1-1 | 0.9 | 0.95 | 0.95 | 1ms |
| R4-P1-05 | 0.9 | 0.95 | 0.95 | 0.5ms |
| R4-P2-2 | 1.1 | 1.05 | 0.8 | 2ms |
| R4-P2-1 | 1.1 | 1.05 | 0.8 | 1ms |
| R4-P2-05 | 1.1 | 1.05 | 0.8 | 0.5ms |

先在每参数点登记2个静态QP检查点（初始、含力阶段），验证维度、参数、请求界与非线性复核；它们是工程预检，不是增加成本候选。检查点必须来自同参数可合法构造状态，不把P0轨迹当P1/P2独立证据。

每条PASS后出单条图并验收，再进入下一条。P1三条完成后立即独立做P1数值比较；P1任一条或配对失败，停止全部剩余C1，不自动跳到P2。P2同理。对每参数点同时报告2→1ms诊断、1→0.5ms决定性门；不得跨参数点或跨候选拼接。P0可补已有同候选2→1ms只读诊断，不为此重跑P0。

单条验收继承当前独立21项检查及新参数身份/绝对截止：完整tick/原始接受子步/solver数量、非有限值、真实力峰/轮胎/支承、请求等于优化首控制、请求和实际转角/角速度边界、源身份/指标复算。原工作力12kN与停止15kN、15°角度及其余冻结界限不扩大；它们是仿真工程界，不是材料强度。2/1/0.5ms比较仅改变内部积分上限。

决定性门：峰值相对差≤2%、世界坐标向量冲量相对差≤2%、终态货物位置≤1mm、包角后的航向≤0.01°。分母及近零策略从现通过比较器原样登记（峰值1N、冲量范数1N·s下限），另报绝对差，不临时换定义。状态一致不能代替输入一致：附报全程最大请求/实际转角差、输入总变差TV、限幅活跃率和尾段放大，不增加事后科学阈值。已观测P0请求差1.30552°、实际转角差0.148898°须在汇总中保留。

产物：每条raw/substeps/solver、metrics、protocol/manifest、single_run_audit、figures及figure_manifest；每参数点r4_parameter_pair.json；最后r4_gate.json逐一链接六条与两组比较。总PASS要求全齐，不能只以6个COMPLETED计数。

## 5. C2/R5：两条合法信息接口验证

前置为新版R4总PASS。目标是从集中全仿真状态转为合法传感测量包的接口转接，不证明分布式网络鲁棒性。植物真值只能进入传感注入器及评价器；控制器读四车本地包与货物协调节点包，保存发送/采样/可用tick、节点、字段、估计和误差日志。

运行前选择新候选P0、2ms已重审的全状态基准（UNFROZEN_FULL01/2ms及U1重审），不得用旧R2C冻结雅可比基准。若新接口算法与该基准数学不一致，先解决身份，不拿误差差异解释为噪声效果。

| 顺序 | 配置 | 特殊验收 |
|---|---|---|
| R5-N0 | 无噪声，同tick融合，P0/2ms，全路线 | 物理/控制白名单字段按既有1e-12绝对界复现同候选基准；时间用新时间合同，墙钟排除；完整单条门和图通过 |
| R5-N1 | 基础噪声，seed5105，其他完全同N0 | 只改变测量噪声；完整单条门通过，报告性能变化，不要求噪声结果复现无噪声 |

基础标准差：位置0.01m、航向0.2°、速度0.02m/s、横摆率0.005rad/s；实际转角暂按零噪声，明确仿真假设而非标定。噪声按节点/字段/绝对tick确定索引，保存或哈希，避免方法提前终止造成随机流错位。航向采用合法因果解缠，含跨π测试；previous_u、参考beta与所有记忆来源登记。

先通过缺节点、未来包、错tick、truth字段、裸ndarray、跨π和噪声复现等负例。N0失败停止N1；N1若违约则失败，不加滤波器或降低噪声掩盖。单种子只用于接口开发，不声明统计鲁棒性。

输出信息可见性图、真值/估计及误差、轨迹/跟踪/构形/力/输入/耗时对照，r5_information_gate.json只接受两个单条及接口审计。R5通过不能直接把P0/P0N正式方法注册表标为已验证分布式方案。

## 6. C3执行器九短片段与C4物理门核销

C3沿原100M05/P0_0.5ms源及已登记SHA，三个窗口为0—2s、11.5—13.5s、16.5—18.5s；每窗执行器更新2/1/0.5ms，各一条，共九条。共同初态、actual_delta及请求序列；控制请求20ms保持，植物内部最大步0.5ms，执行器更新/限幅次序不变。只改变执行器离散更新间隔，不能换新正式植物或直接沿用训练模型声明已验证。

每窗先2ms复现源，再1ms、0.5ms；2ms非时间状态按原1e-12门、时间按已登记合同，失败停止其依赖。每窗比较1→0.5ms原2%/2%/1mm/0.01°门，报告执行器角/速率/限幅和力冲量。九条不含重新求MPC，不按9小时/条估计；启动前小范围估时并登记总2小时上限，任何超时保存原始段并停。

C4建立e01_evidence_matrix：逐行列原E01每项要求、当前证据、plant/输入器/候选/参数/步长身份、科学门、图、剩余动作。已有33条原计划不能简单减去最近运行条数；旧输入器与新控制器不得混算，已失败轨迹保留FAIL，100M05仅代表其子门。转向切换、通信相关植物诊断若未覆盖则按原条目冻结确切数量再执行，不发明一个“已完成E01”总判定。

plant_gate总PASS须逐项证据适用且独立审查通过；E00其他来源缺口不能隐藏。允许单列有限范围PASS，明确尚不支持的域，而不是将一个数值PASS写为所有建模正确。

## 7. K/E02：预测资格与状态闭合

K0先整理既有checkpoint、normalization、S0、模型种子、训练源码和fit/inner/outer/confirmation角色；复用已过元数据链，当前data gate仍PARTIAL的部分补证，不打开未授权确认池。历史训练存在不等于当前控制输入域合格。

K1从记录器定义检查47维状态对实际转角、连接器必要记忆的闭合。用相同可见状态而不同实际转角/合法历史的匹配样本，比较下一状态/力差异；误差阈值与目标预测分辨率事前冻结。若信息不足，先登记模型/状态schema修订及旧权重失效范围，不默默改为51维继续沿用旧权重。

K2按冻结模型逐一重载复现，一步及5/10/20步、40步数值发散监测；包含状态、位置/航向、连接力/内力读出及真实控制梯度。提取并哈希原3%/5%门、12工况、分母和三训练初始化规则，不从MD概述重写有利指标。训练/校准/选择/确认分别登记。

K3资格实验使用同信息、同数据划分、同预算的物理/基础/改进预测器；先原保存数组复算，再必要的缺项补评估。报告所有工况优势及退化，不只总均值。若改进候选不通过，保留合格基础预测器推进允许的控制分支，不能将未通过预测方法包装为主创新。

产物：prediction_identity.json、closure_report、逐轨迹/参数族指标CSV、预测时域与误差分布图、prediction_gate.json。必须说明训练种子数与独立轨迹族数，滑动窗口不是独立样本。

## 8. M/N/B：核心控制和网络证据

M/E03先验收真正多步约束优化、因果网络时间、合法信息和非线性复核；做不可行/未来包/陈旧包/乱序/同tick/回退退出负例。E04仅改变上层架构，在开发集固定后续共同架构；当前全状态诊断上界单独画，不进入正式六方法排名。

| 工作 | 固定对照 | 主要输出/判据 |
|---|---|---|
| E05 | P0、P0N、K0、K0N、K1、K1N：三预测器×网络保护开关 | 同植物/参考/信息/预算/随机族；轨迹、横向误差、内力、逐点原始峰值、完成率及耗时；按原H1—H3判定 |
| E06 | 同模型，只关闭时间对齐；多种时延等级 | AoI/估计误差/入扰—受扰—恢复、横向误差；原H5/H6，不能用全保护开关代替 |
| E07 | 同一误差界和约束收紧，仅换状态均值补偿 | 均值与保守性作用分开；不能让一组使用更大真实信息 |
| E08 | 合格管状/仿射反馈/网络方法及原稿基线 | 原文方法/假设/代码来源/适配差异、原生复现门，再同平台比较；H4只用开发期选定合格强对手 |
| E09 | 随机/突发丢包、时变时延、中断与相关混合压力 | 冻结事件起点/长度/链路/相关性，保存逐包事件；完整恢复段及失败原因，报告压力域 |

每阶段先建机器可读的run_manifest，逐行stage/method/route/family/model_repeat/network/seed/parent/SHA/deadline/expected_figures；检查唯一键、配对齐全、角色合法及数量。保留原书具体网络等级/路线/族数，不将“先小试”混入正式样本。先实施注册的小规模开发/接口验证，方法合格与确认池资格齐备后才冻结正式批次，不自动启动远期14040逻辑/13140最大新增计划。

统计沿原第9节：正式30族的既定平衡、三模型初始化、根种子命名空间核对、轨迹族聚合、成对bootstrap10000次；六主假设同时报告原95%和Bonferroni99.1667%区间，收益≥5%且校正下界>0，并通过原42个护栏。不能把P0/P1/P2三个确定性数值覆盖点称为统计样本或30训练种子。

## 9. A/S/V/T/W：后续支撑证据与删减准则

| 阶段 | 必须验证 | 未完成或失败时如何处理 |
|---|---|---|
| E10 | 观测开关与在线学习开关分离，固定其他模块 | 不将滤波收益归因于在线Koopman适应 |
| E11 | 固定故障注入，检测时延/误报/漏报、容错输入重分配/受力恢复 | 不调故障时刻让本方法胜出；故障检测不能仅以真值标签驱动 |
| E12 | 每项拟写贡献至少一个单模块消融/机制证据 | 无稳定独立收益则删贡献或限定适用域，不强制全部保留 |
| E13 | 开发选参范围、默认值、迟滞、计算分位数/超预算率/消息成本 | 选参和正式测试分离；13秒求解不能写成20ms实时 |
| E14 | 速度×曲率×载荷×网络可行/失败域，控制器所知参数固定 | 缺测区域留空；不以单一2m/s回头弯宣称高速/全域鲁棒 |
| E15 | 独立植物/多体模型或HIL/实测，来源/同步/标定及误差 | 无设备保持RESOURCE_PENDING；模拟力不称实测或材料安全 |
| E16 | 式22、连续输入域/非正规问题、证书假设、递归可行与切换条件 | 已知反例是问题证据，不是证明；无法证明则收缩主张 |
| E17 | 22意见逐条关联最新方法/实验/图/局限/回复；引用与术语核对 | 每条独立关闭，不能因R3通过统一关闭审稿意见 |

E00剩余投稿版本来源、确认盲性、完整mandatory suite等单独列阻塞负责人/可用证据/关闭条件；不把缺失来源当算法失败，也不越过正式数据门。

## 10. 计算预算、恢复和发布节奏

| 批次 | 数量 | 预算安排 |
|---|---:|---|
| C0工程资格 | 只读审计/必要固定QP，无完整路线 | 登记后执行；不无限扫参数 |
| C1/R4 | 6条完整路线 | 单条≤12h，总动态上限72h；名义外推约56h，仅作资源规划 |
| C2/R5 | 2条完整路线 | 单条≤12h，总动态上限24h；两条约19h是粗估，接口/噪声可影响成本 |
| C3 | 9条2s片段 | 总≤2h，依实际无MPC传播估时 |
| C4及K之后 | 按原缺口/注册矩阵确定 | 必须先冻结数量与每条预算，不借本附件全量启动 |

以上是计划上限，不是本次启动授权，也不自动重新授予历史已耗修复预算。R4和R5长路线共8条、单条12h上限合计96h；加C3不超过98h动态预算，工程/出图/审计另记实际耗时。不同参数的计算量不保证等于P0。原则上同机一次一条parallel8路线，避免并发争用令计时不可比。

每条独立进程、隐藏后台、独立日志、绝对截止、每25周期检查点；启动前确认无重复，启动后有限存活/数据增长检查。到期保存TIMEOUT_INCOMPLETE和接受段，停止依赖，不将未应用的solver条目算完成周期。未证明恢复等价，不拼接中断片段；新的重试必须显式核销剩余数量/时间并另run_id。不能用更新任务书反复重置预算。

阶段失败分为身份/记录工程失败、计算超时、物理硬失败、数值比较失败、无统计收益、出图失败；分别记录，不混称“算法不好”。图失败只重做后处理，原仿真不重跑。阶段图未齐不得标交付完成。

## 11. 每种实验完成即出图与交付检查

沿第25.3—25.4的完整映射执行，不另起一个弱化版本。每条动力学至少交付轨迹、误差、力/约束、输入、耗时五类信息，可组合六面板；每组有参数/步长/方法成对图。每个run的PNG+SVG、中文图注、figure_manifest（源/脚本SHA、单位、窗口、统计口径、science_status、QA）必须齐全。

R4单条画四车和货物实际/参考轨迹、各车/货物误差、四连接点Fx/Fy/范数/内力/冲量、轮胎与支承、请求/实际转角和TV、求解耗时；每参数组三步长画数值差与原门。R5加信息时间线、真值—估计和噪声两组对照。C3画请求—实际角/角速度、更新档差异、峰/冲量与事件。K画预测时域/工况/种子误差；网络阶段图同步标攻击、AoI、恢复与失败。

缺Fx/Fy、参考或其他字段时先补记录设计并登记schema，不能从力范数猜分量或画虚构参考。失败/超时画已接受段和终止原因，无数值时画状态/覆盖矩阵。图表统计必须从未平滑数据导出，曲线降采样不得改变独立峰值和冲量。每阶段追加简短报告：做了什么、结果、代价、局限、释放何节点。

## 12. 开始下一阶段前的具体交付

本附件后的第一项实施是C0，不是直接执行旧r4_batch。先交付候选身份迁移及负例、P1/P2绝对参数表、六行run_manifest、独立R4审计/图表收尾接口与预算协议。相应检查全部通过后，才启动唯一R4-P1-2ms；后续按表门控，不越级。

当前文档完成状态：PLAN_READY_FOR_IMPLEMENTATION。C0—C4及其后未因本次文档更新自动完成或启动；现有R3 PASS保持，历史失败保持。
