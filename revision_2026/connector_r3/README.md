# Connector R3 isolated implementation

Protocol SHA256: `dd4b1876cdefc0f55e10999ce0929f21bdc6bafc5cd6443ff1f70c786b774962`.

This directory is the only R3 source/test/script tree. R1/R2 are read-only historical evidence. Results, data, models, and MPC artifacts are stored in sibling versioned directories:

- `connector_r3_results/`
- `connector_r3_data/`
- `connector_r3_models/`
- `connector_r3_mpc/`

The current connector is a numerical planar equivalent without measured hardware calibration. Never describe the 12/15 kN diagnostic thresholds as verified physical strength.
