# 主张模板（分级语言，koopman_ab.md AR1 步骤 4）

## 分级规则（koopman_fix.md 3.3 语言约束）
- L1（可写）："在 N6 训练数据五折持出折上，等预算多时域残差 Koopman（MH16）的 20 步 macro J 相对
  冻结 S0 改善 +20.1%（5/5 折正），D5 困难窗改善 +29.8~47.9%，20 步内 0 发散。"
- L2（回退声明）："在预注册场景级回退规则（触发集 {D1}，规则 R1'，spec SHA 见 ar1/spec_A）下，
  组合预测在全部 12 工况的最差退化 ≤3%（修正口径），macro 改善 +20.4%。"
- L3（不写/禁止）："全域更优"、"系统渐近稳定"、"任何工况不差于 S0"——均无证据，禁止。

## 待 V0/C0 后升格
- V0 十门通过 → 可将 L1 主句主语从"训练折"改为"新 validation（seed 991000，168 轨迹）"。
- C0 通过 → 加 confirm 句。
- D10/D11 改善（+24~32%）与 D1 connector_event 瞬态退化（-3.7~-20.9%，回退后构造性 0）必须同表如实报告（P7）。

## 数字引用规则
- 所有数字必须带 run_id 与文件路径（rows 原始表），禁止摘要替代原始。
- 任何"退化/改善 %"必须标注口径：分母 max(J_s^S0, 0.02) 或 max(|J_s^S0|,1e-12)（E19 修正语义）。
