# Connector V2 workspace

- `src/`: unique versioned connector law, internal-force decomposition and schema.
- `tests/`: automated gate tests.
- `scripts/`: idempotent staged runner.
- `protocol.md`: frozen execution protocol.
- Results, data, models and MPC outputs are stored in sibling versioned directories; V1 artifacts are read-only.

