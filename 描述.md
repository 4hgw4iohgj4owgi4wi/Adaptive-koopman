接下来的实验思路



跟踪路径：/D:/LEARNING/ZNN/ZNN/Adaptive-koopman/Adaptive-koopman-main/paper\_dcn\_tf12\_draft/dcn\_latex\_zh\_comm\_update\_r42\_2026-05-14/generate\_nooffset\_panel\_20260515.py
用这条路径做实验，其中分三种case

case1：单纯的路径跟踪

case2：在弯道逐步加入对通讯的影响的路径跟踪

case3：在弯道注入故障的路径跟踪



base：

系统模型使用之前修改好的四车模型，车内通讯沿用之前的，车辆之间主要交换相对距离和各自航向角；

最基础的使用分布式MPC，里面的模型使用固定参数的单车模型，每个车辆接一个MPC，误差只考虑车辆后轴中心到参考路径的垂向距离以及和相邻车辆的距离，匀速行驶；

控制器输出控制量给系统模型，系统模型计算整体误差、单车误差、货物收力、受力时间等之前我们商量的一系列值，再把分布式MPC需要的辆输出给控制器形成闭环。



baseup：

系统模型使用之前修改好的四车模型，车内通讯沿用之前的，车辆之间主要交换相对距离和各自航向角；同时有个虚拟第五车，作为上层；

控制用上下层设计，上层MPC用四轮转向单车模型，跟踪参考路径，发布各个车辆的参考航向角，这个航向分配同时符合ICR的转向原理；

下四车采用分布式MPC，MPC用固定参数的单车模型，输出控制量给系统模型中的车辆执行，系统模型再反馈数据给控制器；



delay-awareMPC：

系统模型使用之前修改好的四车模型，车内通讯沿用之前的，车辆之间主要交换相对距离和各自航向角；

最基础的使用分布式delay-awareMPC，里面的模型使用固定参数的单车模型，每个车辆接一个delay-awareMPC，误差只考虑车辆后轴中心到参考路径的垂向距离以及和相邻车辆的距离，匀速行驶；

控制器输出控制量给系统模型，系统模型计算整体误差、单车误差、货物收力、受力时间等之前我们商量的一系列值，再把分布式MPC需要的辆输出给控制器形成闭环。



robustMPC：

系统模型使用之前修改好的四车模型，车内通讯沿用之前的，车辆之间主要交换相对距离和各自航向角；

最基础的使用分布式robustMPC，里面的模型使用固定参数的单车模型，每个车辆接一个robustMPC，误差只考虑车辆后轴中心到参考路径的垂向距离以及和相邻车辆的距离，匀速行驶；

控制器输出控制量给系统模型，系统模型计算整体误差、单车误差、货物收力、受力时间等之前我们商量的一系列值，再把分布式MPC需要的辆输出给控制器形成闭环。



delay-awareMPCup：

系统模型使用之前修改好的四车模型，车内通讯沿用之前的，车辆之间主要交换相对距离和各自航向角；同时有个虚拟第五车，作为上层；

控制用上下层设计，上层delay-awareMPC用四轮转向单车模型，跟踪参考路径，发布各个车辆的参考航向角，这个航向分配同时符合ICR的转向原理；

下四车采用分布式delay-awareMPC，delay-awareMPC用固定参数的单车模型，输出控制量给系统模型中的车辆执行，系统模型再反馈数据给控制器；



robustMPCup：

系统模型使用之前修改好的四车模型，车内通讯沿用之前的，车辆之间主要交换相对距离和各自航向角；同时有个虚拟第五车，作为上层；

控制用上下层设计，上层robustMPC用四轮转向单车模型，跟踪参考路径，发布各个车辆的参考航向角，这个航向分配同时符合ICR的转向原理；

下四车采用分布式robustMPC，robustMPC用固定参数的单车模型，输出控制量给系统模型中的车辆执行，系统模型再反馈数据给控制器；



koopman-MPC：

系统模型使用之前修改好的四车模型，车内通讯沿用之前的，车辆之间主要交换相对距离和各自航向角；同时有个虚拟第五车，作为上层；

控制用上下层设计，上层MPC用四轮转向单车模型，跟踪参考路径，发布各个车辆的参考航向角，这个航向分配同时符合ICR的转向原理；

下四车采用分布式koopman-MPC，MPC用Koopman更新参数的单车模型，输出控制量给系统模型中的车辆执行，系统模型再反馈数据给控制器；



koopman-delay-awareMPC：

系统模型使用之前修改好的四车模型，车内通讯沿用之前的，车辆之间主要交换相对距离和各自航向角；同时有个虚拟第五车，作为上层；

控制用上下层设计，上层MPC用四轮转向单车模型，跟踪参考路径，发布各个车辆的参考航向角，这个航向分配同时符合ICR的转向原理；

下四车采用分布式koopman-delay-awareMPC，delay-awareMPC用Koopman更新参数的单车模型，输出控制量给系统模型中的车辆执行，系统模型再反馈数据给控制器；



koopman-MPC-ob：

系统模型使用之前修改好的四车模型，车内通讯沿用之前的，车辆之间主要交换相对距离和各自航向角；同时有个虚拟第五车，作为上层；

控制用上下层设计，上层MPC用四轮转向单车模型，跟踪参考路径，发布各个车辆的参考航向角，这个航向分配同时符合ICR的转向原理；

下四车采用分布式koopman-MPC，MPC用koopman自适应观测器的更新的参数的单车模型，输出控制量给系统模型中的车辆执行，系统模型再反馈数据给控制器；



koopman-MPC-ob-Orin:

系统模型使用之前修改好的四车模型，车内通讯沿用之前的，车辆之间主要交换相对距离和各自航向角；同时有个虚拟第五车，作为上层；

控制用上下层设计，上层MPC用四轮转向单车模型，跟踪参考路径，发布各个车辆的参考航向角，这个航向分配同时符合ICR的转向原理；

下四车采用分布式koopman-MPC，MPC用koopman自适应观测器的更新的参数的单车模型，输出控制量给系统模型中的车辆执行，系统模型再反馈数据给控制器；

同时MPC启动之前文章内设计的所有的模块（PPC，FTC/FDI，communication-quality scheduling，delay compensation）。







